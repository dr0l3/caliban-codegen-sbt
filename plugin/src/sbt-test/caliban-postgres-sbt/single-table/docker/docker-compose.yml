version: "3.3"
services:
  postgres:
    build:
      context: .
    environment:
      - "POSTGRES_PASSWORD=postgres"
      - "POSTGRES_DB=product"
      - "POSTGRES_USER=postgres"
    ports:
      - "${POSTGRES_PORT}:5432"