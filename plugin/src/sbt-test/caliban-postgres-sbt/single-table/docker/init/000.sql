\connect product

create table public.product(
                               upc text primary key,
                               name text,
                               price integer,
                               weight integer
);

